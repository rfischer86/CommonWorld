export enum FormTypes {
  textField = 'textField',
  textLine = 'textLine',
  textArea = 'textArea',
  date = 'date',
  number = 'number',
  checkbox = 'checkbox',
  select = 'select',
  range = 'range',
  document = 'document',
  image = 'image',
  color = 'color'
}

